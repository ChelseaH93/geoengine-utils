"""Logging helpers."""

from typing import Any


def log_message(message: Any) -> str:
    """Format a message for logging.

    Parameters
    ----------
    message : Any
        Message content.

    Returns
    -------
    str
        String representation of the message.
    """

    return str(message)
