"""Vector validation helpers."""

from typing import Any


def validate_vector(data: Any) -> bool:
    """Placeholder validator for vector data.

    Parameters
    ----------
    data : Any
        Vector data payload to validate.

    Returns
    -------
    bool
        Always ``True`` until a concrete validator is implemented.
    """

    return True
